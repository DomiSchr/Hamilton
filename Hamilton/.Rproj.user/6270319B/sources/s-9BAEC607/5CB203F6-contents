#Hamilton-Method for the Apportionment Problem

hamilton <- function(p, H){
  
  P <- sum(p)
  A <- P/H
  
  share <- array(0, length(p))
  
  
  for(i in 1:length(p)){
    share[i] <- p[i]/A
  }
  
  
  return(share[1])
}


p <- c(630, 480, 390, 500)

hamilton(p, 10)
